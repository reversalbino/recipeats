extends layout.pug 

block content 

    if recipes.length > 0
        ul Recipes 
            for recipe in recipes 
                li: a.recipe-container(href=`/recipes/${recipe.id}`)=recipe.dataValues.title
    else 
        p You have no recipes on this board :(