extends layout.pug 

block content 

    if recipesOnSpecificBoard.Recipes.length > 0
        ul Recipes 
            for recipe in recipesOnSpecificBoard.Recipes
                li: a.recipe-container(href=`/recipes/${recipe.id}`)=recipe.title
    else 
        p You have no recipes on this board :(